import { Injectable } from '@nestjs/common'
// import { User } from './user.entity'

@Injectable()
export class UserService {
  //   public async findByLogin(data: {login: string}): Promise<User> {}
  //   public async findByEmail(data: {email: string}): Promise<User> {}
  //   public async fintByPhone(data: {phone: string}): Promise<User> {}
}
